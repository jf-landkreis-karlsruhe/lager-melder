apiVersion: v1
kind: Service
metadata:
  name: {{ include "lager-melder.fullname" . }}
  labels:
    {{- include "lager-melder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lager-melder.selectorLabels" . | nindent 4 }}
