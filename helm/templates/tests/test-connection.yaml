apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lager-melder.fullname" . }}-test-connection"
  labels:
    {{- include "lager-melder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lager-melder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
